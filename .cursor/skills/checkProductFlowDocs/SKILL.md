---
name: checkProductFlowDocs
description: Verify product flow docs match code. Run npm run check:product-flow-docs at repo root.
---

Run from repo root:

```bash
npm run check:product-flow-docs
```

Fix drift in `docs/flows/` or update API route registrations to match `@flex/shared/dictionary/apiRoutes.dictionary.ts`.
