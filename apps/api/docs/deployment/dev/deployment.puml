````puml
``` TODO: Figure out why including puml in the project causes it to fail during the build on Digital Ocean
``` Removing it from the src folder has no effect
``` Adding it to the ignore list in tsup.config.ts has no effect

@startuml Project Development Deployment Flow

skinparam backgroundColor white
skinparam handwritten false
skinparam defaultFontName Arial
skinparam activityBorderColor #666666
skinparam activityBackgroundColor #EEEEEE

|Developer|
start
:Pull latest dev branch;
:Create feature branch;
:Push feature branch;
:Create Pull Request;

|GitHub Actions|
:Run automated checks;
fork
  :Lint check;
fork again
  :Test check;
end fork
if (Checks passed?) then (yes)
  :Await review approval;
else (no)
  :Fix issues;
  stop
endif

|Developer|
if (PR approved?) then (yes)
  :Merge to dev branch;
else (no)
  :Address review comments;
  stop
endif

|GitHub Actions|
:Run migrate-and-seed job;
fork
  :Attempt migration;
  if (Migration successful?) then (yes)
  else (no)
    :Try database reset;
  endif
  :Seed database;
end fork

|Digital Ocean|
:Detect dev branch changes;
:Build using Dockerfile;
:Apply environment variables\nfrom Digital Ocean settings;
:Deploy to dev.hourrier.appshop.biz/api;

stop

@enduml
````
