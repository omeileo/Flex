import { RequestOfferRefundRequestBody } from './refund.types'

export const refundService = {
  requestRefundForOfferRequest: async (refundRequest: RequestOfferRefundRequestBody) => {
    // const currentUser = getCurrentLoggedInUserOrThrow()
    // const refund = await refundRepository.createRefundForOfferRequest(
    //   currentUser.userId,
    //   refundRequest
    // )
    // return refund
  }
}
