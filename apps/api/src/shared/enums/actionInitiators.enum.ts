/**
 * Represents the entities that can initiate actions in the system.
 */
export const enum ActionInitiators {
  /**
   * Actions initiated by a user.
   */
  User = 'user',

  /**
   * Actions initiated by the system.
   */
  System = 'system'
}
